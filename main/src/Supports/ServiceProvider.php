<?php

namespace Kernery\Main\Supports;

use Illuminate\Support\ServiceProvider as AppServiceProvider;

class ServiceProvider extends AppServiceProvider
{
    /*
    * * All Kernery service providers extends to this app service provider
    * * This provider uses laravel's service provider that helps to mount
    * * all providers within the core directory into laravel's own
    */
}
